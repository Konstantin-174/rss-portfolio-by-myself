import React from 'react';

export const Contacts = () => {
    return (
        <section id="contacts">
            <h2 className="section-title">
                Contact me
            </h2>
        </section>
    )
}