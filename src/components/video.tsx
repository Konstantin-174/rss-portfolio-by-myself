import React from 'react';

export const Video = () => {
    return (
        <section id="video">
            <h2 className="section-title">
                Video
            </h2>
        </section>
    )
}