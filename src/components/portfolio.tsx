import React from 'react';

export const Portfolio = () => {
    return (
        <section id="portfolio">
            <h2 className="section-title">
                Portfolio
            </h2>
        </section>
    )
}